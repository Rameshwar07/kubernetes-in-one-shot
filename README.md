### STEPS
Markdown : - --> Run install.sh first (kubectl,KIND,Docker installation)  

--> Run kind create cluster --name tws-cluster --config=config.yml (On Kind cluster creates node)
--> After that you can work accordingly
